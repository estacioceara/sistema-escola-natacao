<?php
// Preparação dos dados
?>

<section class="container py-5">
    <div class="card shadow-lg border-0 rounded-4 p-4 main-card-planos">
        <!-- Cabeçalho da página e botão de adicionar aluno -->
        <div class="row align-items-center mb-4 g-2">
            <div class="col-12 col-md-7 d-flex align-items-center gap-2 justify-content-center justify-content-md-start">
                <span class="material-icons text-primary fs-1" style="font-size:2.2rem;">school</span>
                <h1 class="mb-0 fw-bold text-primary" style="font-size:2rem;">Alunos</h1>
            </div>
            <div class="col-12 col-md-5 d-flex justify-content-center justify-content-md-end">
                <a href="/alunos/adicionar" class="btn btn-primary d-flex align-items-center gap-2 w-80 w-md-auto" style="max-width:260px;">
                    <span class="material-icons">add</span> Adicionar Aluno
                </a>
            </div>
        </div>

        <!-- Mensagem caso não haja alunos -->
        <?php if (empty($students)): ?>
            <div class="alert alert-info d-flex align-items-center gap-2">
                <span class="material-icons">info</span>
                Nenhum aluno cadastrado.
            </div>
        <?php else: ?>
            <!-- Tabela de alunos -->
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>ID</th>
                            <th>Nome do Aluno</th>
                            <th>CPF</th>
                            <th>Telefone</th>
                            <th class="text-center">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($students as $student): ?>
                            <tr>
                                <td><?= htmlspecialchars($student->getId()) ?></td>
                                <td class="fw-semibold text-primary">
                                    <?= htmlspecialchars($student->getName()) ?>
                                </td>
                                <td><?= htmlspecialchars($student->getCpf() ?? 'N/A') ?></td>
                                <td><?= htmlspecialchars($student->getPhone()) ?></td>

                                <td class="text-center">
                                    <div class="btn-group" role="group" aria-label="Ações do aluno">
                                        <a href="/alunos/view?id=<?= urlencode($student->getId()) ?>" class="btn btn-sm btn-info d-flex align-items-center gap-1">
                                            <span class="material-icons" style="font-size:1.1rem;">visibility</span>
                                            <span>Ver</span>
                                        </a>
                                        <a href="/alunos/editar?id=<?= urlencode($student->getId()) ?>" class="btn btn-sm btn-warning d-flex align-items-center gap-1">
                                            <span class="material-icons" style="font-size:1.1rem;">edit</span>
                                            <span>Editar</span>
                                        </a>
                                        <a href="/alunos/excluir?id=<?= urlencode($student->getId()) ?>" class="btn btn-sm btn-danger d-flex align-items-center gap-1"
                                            onclick="return confirm('Tem certeza que deseja excluir este aluno?')">
                                            <span class="material-icons" style="font-size:1.1rem;">delete</span>
                                            <span>Excluir</span>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        <?php endif; ?>
    </div>
</section>
